import { FilterListPostPipe } from './filter-list-post.pipe';

describe('FilterListPostPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListPostPipe();
    expect(pipe).toBeTruthy();
  });
});
