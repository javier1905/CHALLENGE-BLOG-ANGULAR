import { FiterTodoPipe } from './fiter-todo.pipe';

describe('FiterTodoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterTodoPipe();
    expect(pipe).toBeTruthy();
  });
});
