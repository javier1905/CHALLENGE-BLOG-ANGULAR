import { FilterListTodoPipe } from './filter-list-todo.pipe';

describe('FilterListTodoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListTodoPipe();
    expect(pipe).toBeTruthy();
  });
});
